# Copy to ~/.config/nautilus-trader/alpaca/alerts.env.
# Keep this file outside git and chmod 600.
#
# Candidate Discord alerts are emitted by the sidecar command:
#   alpaca-control alerts candidates --all --send
#
# DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...
