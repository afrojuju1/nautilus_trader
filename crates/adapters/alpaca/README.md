# Nautilus Alpaca Adapter

This crate is the scaffold for an Alpaca Markets adapter. It currently provides shared
configuration, environment, feed, and URL definitions used by the Python adapter package and the
planned Rust live data and execution clients.

The implementation plan is tracked in `docs/developer_guide/alpaca_adapter_runtime_plan.md`.
